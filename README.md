# NumPy-NN-from-scratch
a small feedforward network with manual forward pass, backprop, and at least two optimizers (SGD with momentum, Adam) implemented, no PyTorch/TensorFlow. Trained it on MNIST or a small synthetic dataset.
